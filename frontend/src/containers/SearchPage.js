import { useData } from './hooks/useContext';

const SearchPage = () => {

    const { x } = useData()

    return (
        <div>
            TODO 搜尋結果頁面
        </div>
    )
}

export default SearchPage;