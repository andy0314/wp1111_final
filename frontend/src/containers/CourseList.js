import { useData } from './hooks/useContext'

const CourseList = () => {

    const { x } = useData()
    return (
        <div>
            TODO 志願序排序
        </div>
    )
}

export default CourseList;